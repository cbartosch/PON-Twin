# Copy this file to ".env" and paste your key. The .env is git/zip-excluded.
# Used only by the Ask (LLM) tab of the Streamlit app for OpenAI tool-calling.
OPENAI_API_KEY=sk-your-openai-key-here
# Optional: override the default chat model (sidebar default is gpt-5.4)
# OPENAI_MODEL=gpt-4o
